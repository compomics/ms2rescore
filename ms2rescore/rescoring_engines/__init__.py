"""
Rescoring engines integrated in MS²Rescore.

Each integrated rescoring engine typically includes a :py:func:`rescore` function that takes a
:py:class:`~psm_utils.psm_list.PSMList` as input and writes the new scores, q-values, and PEPs to
the original :py:class:`~psm_utils.psm_list.PSMList`.

"""
